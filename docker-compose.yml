services:
  backend:
    build:
      context: ./server
    container_name: lms-backend
    ports:
      - "8000:8000"
    env_file:
      - ./server/.env
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: lms-redis
    ports:
      - "6379:6379"